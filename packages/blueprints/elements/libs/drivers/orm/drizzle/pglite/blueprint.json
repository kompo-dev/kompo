{
  "env": {
    "DATABASE_URL": {
      "description": "PGlite connection string (memory:// or file://path or idb://)",
      "validation": "z.string()",
      "side": "client",
      "mapTo": "url",
      "default": "idb://"
    }
  },
  "id": "pglite",
  "name": "pglite"
}
