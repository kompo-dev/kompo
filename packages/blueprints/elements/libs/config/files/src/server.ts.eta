/**
 * Server-side environment configuration
 */
import { createEnv } from '@kompo/config/server'
import { serverSchema } from './schema'

export { createEnv }

/**
 * Server-side environment singleton
 */
export const serverEnv = createEnv({
  server: serverSchema,
  client: {},
  runtimeEnv: process.env,
})
